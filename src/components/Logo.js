import React from "react";
import logo from '../logo.svg';

function Logo() {
    return(
        <div>
            <img src={logo} className="App-logo" alt="logo"/>
        </div>
    )
};

export default Logo;